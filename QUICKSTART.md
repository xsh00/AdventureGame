# 快速启动指南

## 开发模式运行

```bash
cd E:\code\AdventureGame
npm run dev
```

然后访问: http://localhost:3000

## 生产构建

```bash
npm run build
```

构建产物在 `dist/` 目录,可以直接部署到任何静态Web服务器。

## 部署到Reverie游戏平台

1. 执行构建:
```bash
npm run build
```

2. 将 `dist/` 目录的所有文件复制到Reverie的 `public/games/adventure-game-vue/` 目录

3. 在Reverie数据库注册游戏:
```sql
INSERT INTO games (id, name, description, cover_image, game_url, credits_per_play, is_active)
VALUES (
    'adventure-game-vue',
    '神秘森林冒险(Vue版)',
    '一个AI驱动的文字冒险游戏 - Vue3 + TypeScript实现',
    '/images/game-covers/adventure-game.jpg',
    '/games/adventure-game-vue/index.html',
    0,
    true
);
```

4. 访问 Reverie 游戏区即可开始游玩!

## 本地测试(独立运行)

游戏支持离线模式,即使不在Reverie平台内也能运行:

```bash
npm run dev
```

5秒后会自动切换到离线模式,所有核心功能(探索、战斗、商店、升级)都能正常使用,只是AI功能不可用。

## 预览生产构建

```bash
npm run preview
```

## 目录结构

```
E:\code\AdventureGame/
├── dist/                    # 构建产物(npm run build生成)
├── src/
│   ├── components/          # Vue组件
│   ├── composables/         # 游戏逻辑
│   ├── stores/              # 状态管理
│   ├── types/               # TypeScript类型
│   ├── utils/               # 工具函数(Reverie SDK)
│   ├── App.vue              # 主应用
│   └── main.ts              # 入口
├── index.html
├── package.json
├── vite.config.ts
└── README.md               # 完整文档
```

## 游戏特性

- ✅ 森林探索系统
- ✅ 回合制战斗
- ✅ 村庄商店和旅馆
- ✅ 技能学习系统
- ✅ 角色升级系统
- ✅ Reverie AI集成
- ✅ 离线模式支持
- ✅ 游戏存档系统
- ✅ 响应式设计(PC/平板/手机)

## 技术栈

- Vue 3.4
- TypeScript 5.3
- Pinia 2.1 (状态管理)
- Vite 5.0 (构建工具)
- 原生CSS (无UI框架)

## 注意事项

1. **类型检查**: 如需进行完整的TypeScript类型检查,使用:
   ```bash
   npm run build:check
   ```

2. **Reverie集成**: 游戏使用postMessage与Reverie平台通信,必须作为iframe运行才能使用AI功能。

3. **存档功能**: 存档保存在Reverie平台数据库,本地开发模式下存档功能会失败(但不影响游戏运行)。

4. **浏览器兼容**: 推荐使用现代浏览器(Chrome/Edge/Firefox/Safari最新版)。

## 常见问题

**Q: 游戏提示"尚未连接到 Reverie 平台"?**
A: 正常现象。5秒后会自动切换到离线模式,游戏可以正常进行。

**Q: 如何测试AI功能?**
A: 需要将游戏部署到Reverie平台并在iframe内运行。

**Q: 可以自定义游戏内容吗?**
A: 可以!修改 [src/composables/useGameActions.ts](src/composables/useGameActions.ts) 即可添加新的敌人、事件、物品等。

## 开发建议

- 使用 VSCode + Volar 插件获得最佳开发体验
- 启用 TypeScript 严格模式检查
- 使用 Vue DevTools 调试状态

---

**祝你开发愉快!** 🎮✨
